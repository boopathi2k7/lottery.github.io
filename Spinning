import { render, screen } from "@testing-library/react";
import App from "./App";

test("renders Navbar component", () => {
  render(<App />);
  const navbarElement = screen.getByRole("navigation"); // Assuming Navbar has a role="navigation"
  expect(navbarElement).toBeInTheDocument();
});
